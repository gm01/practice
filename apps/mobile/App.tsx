import { useMemo, useState } from "react";
import { ActivityIndicator, Image, Keyboard, KeyboardAvoidingView, Platform, Pressable, ScrollView, StatusBar, Text, TextInput, TouchableWithoutFeedback, View } from "react-native";
import { SafeAreaProvider, SafeAreaView } from "react-native-safe-area-context";
import { fetchDashboard } from "./src/api";
import { C, s } from "./src/styles";
import type { Dashboard, Match, Player, Side } from "./src/types";

const positionCoordinates:Record<number,[number,number]>={0:[50,90],1:[50,81],2:[88,75],3:[82,78],4:[66,77],5:[50,78],6:[34,77],7:[18,78],8:[12,70],9:[65,64],10:[50,65],11:[35,64],12:[84,54],13:[66,53],14:[50,54],15:[34,53],16:[16,54],17:[68,42],18:[50,43],19:[32,42],20:[67,28],21:[50,30],22:[33,28],23:[84,22],24:[63,17],25:[50,16],26:[37,17],27:[16,22]};

const average=(v:number[])=>v.length?v.reduce((a,b)=>a+b,0)/v.length:0;
const percent=(v:number,t:number)=>t?Math.round(v/t*100):0;
const date=(v:string)=>new Date(v.endsWith("Z")?v:`${v}Z`).toLocaleDateString("ko-KR",{timeZone:"Asia/Seoul"});

function PlayerImage({player,size=52}:{player:Player;size?:number}){const[index,setIndex]=useState(0),[failed,setFailed]=useState(false);return <View style={[s.photo,{width:size,height:size,borderRadius:size/2}]}>{failed?<Text style={s.photoText}>{player.name.slice(0,1)}</Text>:<Image source={{uri:player.imageUrls[index]}} style={{width:size,height:size}} resizeMode="contain" onError={()=>index+1<player.imageUrls.length?setIndex(index+1):setFailed(true)}/>}</View>}
function Back({onPress}:{onPress:()=>void}){return <Pressable onPress={onPress} style={s.back}><Text style={s.backText}>← 뒤로</Text></Pressable>}
function Result({value}:{value:string}){const color=value==="승"?C.green:value==="패"?C.red:C.muted;return <View style={[s.result,{borderColor:color}]}><Text style={{color,fontWeight:"900"}}>{value}</Text></View>}
function Kpi({label,value,note}:{label:string;value:string;note:string}){return <View style={s.kpi}><Text style={s.label}>{label}</Text><Text style={s.kpiValue}>{value}</Text><Text style={s.muted}>{note}</Text></View>}
function PlayerRow({player,onPress}:{player:Player;onPress:()=>void}){return <Pressable style={s.playerRow} onPress={onPress}><PlayerImage player={player}/><View style={s.flex}><Text style={s.playerName}>{player.name}</Text><Text style={s.muted}>{player.position} · +{player.grade}강</Text><Text style={s.green}>{player.goals}골 {player.assists}도움</Text></View><Text style={s.rating}>{player.rating.toFixed(1)}</Text><Text style={s.arrow}>→</Text></Pressable>}

function PitchLines(){return <><View style={s.halfway}/><View style={s.centerCircle}/><View style={s.centerSpot}/><View style={s.topPenalty}/><View style={s.topGoalArea}/><View style={s.bottomPenalty}/><View style={s.bottomGoalArea}/></>}

function Formation({players,onPlayer}:{players:Player[];onPlayer:(id:number)=>void}){return <View style={s.formationPitch}><PitchLines/>{players.filter(p=>p.rating>0&&p.positionCode<28).map(p=>{const[x,y]=positionCoordinates[p.positionCode]??[50,50];return <Pressable style={[s.formationPlayer,{left:`${x}%`,top:`${y}%`}]} onPress={()=>onPlayer(p.spId)} key={p.spId}><View><PlayerImage player={p} size={38}/><Text style={s.formationRating}>{p.rating.toFixed(1)}</Text>{(p.goals>0||p.assists>0)&&<View style={s.playerEvents}><Text style={s.eventIcon}>{"⚽".repeat(p.goals)}</Text><Text style={s.eventIcon}>{"👟".repeat(p.assists)}</Text></View>}</View><Text style={s.formationName} numberOfLines={1}>{p.name}</Text><Text style={s.formationMeta}>{p.position} · +{p.grade}</Text></Pressable>})}</View>}

function Home({data,onMatch,onPlayer}:{data:Dashboard;onMatch:(m:Match)=>void;onPlayer:(id:number,side:Side)=>void}){
  const report=useMemo(()=>{const wins=data.matches.filter(m=>m.result==="승").length,goals=data.matches.reduce((a,m)=>a+m.myScore,0),against=data.matches.reduce((a,m)=>a+m.opponentScore,0),shots=data.matches.reduce((a,m)=>a+m.stats.shots,0);return{wins,goals,against,shots}},[data]);
  const players=useMemo(()=>{const map=new Map<number,{p:Player;games:number;rating:number;goals:number;assists:number}>();data.matches.forEach(m=>m.players.filter(p=>p.rating>0).forEach(p=>{const r=map.get(p.spId)??{p,games:0,rating:0,goals:0,assists:0};r.games++;r.rating+=p.rating;r.goals+=p.goals;r.assists+=p.assists;map.set(p.spId,r)}));return[...map.values()].sort((a,b)=>b.games-a.games||b.rating/b.games-a.rating/a.games).slice(0,8)},[data]);
  return <ScrollView contentContainerStyle={s.content}><View style={s.profile}>{players[0]&&<PlayerImage player={players[0].p} size={66}/>}<View style={s.flex}><Text style={s.eyebrow}>CLUB OWNER</Text><Text style={s.profileName}>{data.profile.nickname}</Text><Text style={s.muted}>LV.{data.profile.level.toLocaleString()} · {data.profile.divisionName}</Text></View></View><Text style={s.heading}>최근 {data.matches.length}경기 리포트</Text><View style={s.grid}><Kpi label="승률" value={`${percent(report.wins,data.matches.length)}%`} note={`${report.wins}승`}/><Kpi label="평균 득실" value={`${average(data.matches.map(m=>m.myScore)).toFixed(1)} : ${average(data.matches.map(m=>m.opponentScore)).toFixed(1)}`} note={`득실차 ${report.goals-report.against}`}/><Kpi label="슛 전환율" value={`${percent(report.goals,report.shots)}%`} note={`${report.shots}슛 · ${report.goals}골`}/><Kpi label="유효 슈팅" value={`${percent(data.matches.reduce((a,m)=>a+m.stats.effectiveShots,0),report.shots)}%`} note="전체 슈팅 기준"/></View><Text style={s.heading}>선수 누적</Text><ScrollView horizontal showsHorizontalScrollIndicator={false} contentContainerStyle={s.horizontal}>{players.map(r=><Pressable style={s.playerCard} key={r.p.spId} onPress={()=>onPlayer(r.p.spId,"mine")}><PlayerImage player={r.p}/><Text style={s.playerName} numberOfLines={1}>{r.p.name}</Text><Text style={s.muted}>{r.games}경기 · {(r.rating/r.games).toFixed(1)}</Text><Text style={s.green}>{r.goals}골 {r.assists}도움</Text></Pressable>)}</ScrollView><Text style={s.heading}>최근 경기</Text>{data.matches.map((m,i)=><Pressable style={s.matchRow} key={m.id} onPress={()=>onMatch(m)}><Text style={s.matchNo}>{String(i+1).padStart(2,"0")}</Text><Result value={m.result}/><Text style={s.score}>{m.myScore} : {m.opponentScore}</Text><View style={[s.flex,s.right]}><Text style={s.playerName}>VS {m.opponentNickname}</Text><Text style={s.muted}>{date(m.matchDate)}</Text></View><Text style={s.arrow}>→</Text></Pressable>)}{data.warnings.length>0&&<Text style={s.warning}>일부 경기 {data.warnings.length}건을 불러오지 못했습니다.</Text>}</ScrollView>
}

function MatchScreen({match,nickname,onBack,onPlayer}:{match:Match;nickname:string;onBack:()=>void;onPlayer:(id:number,side:Side)=>void}){
  const[tab,setTab]=useState<"요약"|"통계"|"라인업"|"슛맵">("요약"),[side,setSide]=useState<Side>("mine"),[selectedShot,setSelectedShot]=useState<{shot:Match["shots"][number];side:Side}|null>(null);const mine=match.players.filter(p=>p.rating>0).sort((a,b)=>b.rating-a.rating),away=match.opponentPlayers.filter(p=>p.rating>0).sort((a,b)=>b.rating-a.rating);
  return <ScrollView contentContainerStyle={s.content}><Back onPress={onBack}/><Text style={s.centerMuted}>{date(match.matchDate)} · {match.controller}</Text><View style={s.scoreHero}><View style={s.club}><Text style={s.clubName}>{nickname}</Text><Text style={s.muted}>{match.divisionName}</Text></View><Text style={s.heroScore}>{match.myScore} : {match.opponentScore}</Text><View style={s.club}><Text style={s.clubName}>{match.opponentNickname}</Text><Text style={s.muted}>{match.opponentDivisionName}</Text></View></View><View style={s.tabs}>{(["요약","통계","라인업","슛맵"] as const).map(t=><Pressable style={[s.tab,tab===t&&s.activeTab]} onPress={()=>setTab(t)} key={t}><Text style={tab===t?s.green:s.muted}>{t}</Text></Pressable>)}</View>{tab==="요약"&&<><Text style={s.heading}>득점 타임라인</Text>{match.goals.map((g,i)=><View style={s.goalRow} key={`${g.minute}-${i}`}><Text style={[s.minute,{color:g.side==="mine"?C.green:C.blue}]}>{g.minute}'</Text><View style={s.flex}><Text style={s.playerName}>⚽ {g.playerName}</Text>{g.assistName&&<Text style={s.muted}>도움 · {g.assistName}</Text>}</View></View>)}{match.ownGoalsFor>0&&<Text style={s.warning}>상대 자책골 {match.ownGoalsFor} · 시간 정보 없음</Text>}{match.ownGoalsAgainst>0&&<Text style={s.warning}>내 자책골 {match.ownGoalsAgainst} · 시간 정보 없음</Text>}<Text style={s.heading}>최고 평점 선수</Text>{mine.slice(0,3).map(p=><PlayerRow player={p} onPress={()=>onPlayer(p.spId,"mine")} key={p.spId}/>)}</>}{tab==="통계"&&<View style={s.stats}>{[["점유율",match.stats.possession,match.opponentStats.possession,"%"],["슈팅",match.stats.shots,match.opponentStats.shots,""],["유효 슈팅",match.stats.effectiveShots,match.opponentStats.effectiveShots,""],["패스 성공률",match.stats.passAccuracy,match.opponentStats.passAccuracy,"%"],["자책골",match.ownGoalsAgainst,match.ownGoalsFor,""]].map(([l,a,b,u])=><View style={s.stat} key={String(l)}><Text style={s.statValue}>{String(a)}{u}</Text><Text style={s.muted}>{l}</Text><Text style={s.statValue}>{String(b)}{u}</Text></View>)}</View>}{tab==="라인업"&&<><View style={s.sideTabs}><Pressable onPress={()=>setSide("mine")}><Text style={side==="mine"?s.green:s.muted}>{nickname}</Text></Pressable><Pressable onPress={()=>setSide("opponent")}><Text style={side==="opponent"?s.blue:s.muted}>{match.opponentNickname}</Text></Pressable></View><Formation players={side==="mine"?mine:away} onPlayer={id=>onPlayer(id,side)}/></>}{tab==="슛맵"&&<><View style={s.shotLegend}><Text style={s.green}>○ 내 슈팅</Text><Text style={s.blue}>○ 상대 슈팅</Text><Text style={s.muted}>● 득점</Text></View><View style={s.pitch}><PitchLines/>{match.shots.map((x,i)=><Pressable hitSlop={9} onPress={()=>setSelectedShot({shot:x,side:"mine"})} style={[s.shot,{left:`${x.y*100}%`,top:`${(1-x.x)*100}%`},x.isGoal&&s.goalShot]} key={`m${i}`}/>)}{match.opponentShots.map((x,i)=><Pressable hitSlop={9} onPress={()=>setSelectedShot({shot:x,side:"opponent"})} style={[s.shot,{left:`${(1-x.y)*100}%`,top:`${x.x*100}%`,borderColor:C.blue},x.isGoal&&s.goalShot]} key={`a${i}`}/>)}</View>{selectedShot&&<View style={s.shotDetail}><View style={[s.shotBadge,{backgroundColor:selectedShot.side==="mine"?C.green:C.blue}]}><Text style={s.shotBadgeText}>{selectedShot.shot.minute}'</Text></View><View style={s.flex}><Text style={s.playerName}>{selectedShot.shot.isGoal?"⚽ 득점":"슈팅"} · {selectedShot.shot.playerName}</Text><Text style={s.muted}>{selectedShot.side==="mine"?nickname:match.opponentNickname}{selectedShot.shot.assistName?` · 도움 ${selectedShot.shot.assistName}`:""}</Text></View></View>}</>}</ScrollView>
}

function PlayerScreen({matches,id,side,onBack,onMatch}:{matches:Match[];id:number;side:Side;onBack:()=>void;onMatch:(m:Match)=>void}){const rows=matches.flatMap(match=>{const player=(side==="mine"?match.players:match.opponentPlayers).find(p=>p.spId===id&&p.rating>0);return player?[{match,player}]:[]}),player=rows[0]?.player;if(!player)return <View style={s.content}><Back onPress={onBack}/><Text style={s.heading}>출전 기록 없음</Text></View>;const goals=rows.reduce((a,r)=>a+r.player.goals,0),assists=rows.reduce((a,r)=>a+r.player.assists,0),shots=rows.reduce((a,r)=>a+r.player.shots,0),passTry=rows.reduce((a,r)=>a+r.player.passTry,0),passSuccess=rows.reduce((a,r)=>a+r.player.passSuccess,0);return <ScrollView contentContainerStyle={s.content}><Back onPress={onBack}/><View style={s.playerHero}><PlayerImage player={player} size={90}/><View style={s.flex}><Text style={s.eyebrow}>PLAYER REPORT</Text><Text style={s.playerHeroName}>{player.name}</Text><Text style={s.muted}>{player.seasonName} · {player.position} · +{player.grade}강</Text></View><Text style={s.heroRating}>{average(rows.map(r=>r.player.rating)).toFixed(2)}</Text></View><View style={s.grid}><Kpi label="출전" value={`${rows.length}`} note="불러온 경기"/><Kpi label="골 · 도움" value={`${goals} · ${assists}`} note={`경기당 ${((goals+assists)/rows.length).toFixed(2)}P`}/><Kpi label="슈팅" value={`${shots}`} note={`유효 ${percent(rows.reduce((a,r)=>a+r.player.effectiveShots,0),shots)}%`}/><Kpi label="패스" value={`${percent(passSuccess,passTry)}%`} note={`${passSuccess}/${passTry}`}/></View><Text style={s.heading}>최근 평점</Text><View style={s.bars}>{rows.slice(0,10).map((r,i)=><Pressable style={s.barWrap} onPress={()=>onMatch(r.match)} key={r.match.id}><Text style={s.muted}>{r.player.rating.toFixed(1)}</Text><View style={[s.bar,{height:Math.max(18,r.player.rating*14)}]}/><Text style={s.muted}>{i+1}</Text></Pressable>)}</View><Text style={s.heading}>출전 경기</Text>{rows.map(r=><Pressable style={s.matchRow} onPress={()=>onMatch(r.match)} key={r.match.id}><Result value={side==="mine"?r.match.result:r.match.result==="승"?"패":r.match.result==="패"?"승":"무"}/><View style={s.flex}><Text style={s.playerName}>VS {r.match.opponentNickname}</Text><Text style={s.muted}>{date(r.match.matchDate)} · {r.player.position}</Text></View><Text style={s.green}>{r.player.goals}G {r.player.assists}A</Text><Text style={s.rating}>{r.player.rating.toFixed(1)}</Text></Pressable>)}</ScrollView>}

export default function App(){const[apiKey,setApiKey]=useState(""),[nickname,setNickname]=useState(""),[data,setData]=useState<Dashboard|null>(null),[loading,setLoading]=useState(false),[error,setError]=useState(""),[match,setMatch]=useState<Match|null>(null),[player,setPlayer]=useState<{id:number;side:Side}|null>(null);async function load(){if(!apiKey.trim()||!nickname.trim()){setError("API 키와 구단주명을 입력해 주세요.");return}setLoading(true);setError("");try{setData(await fetchDashboard(apiKey,nickname))}catch(e){setError(e instanceof Error?e.message:"전적 조회 실패")}finally{setLoading(false)}}return <SafeAreaProvider><SafeAreaView style={s.safe}><StatusBar barStyle="light-content"/><View style={s.header}><Text style={s.logo}>FC ONLINE LAB</Text><Text style={s.green}>MOBILE</Text></View><KeyboardAvoidingView style={s.flex} behavior={Platform.OS==="ios"?"padding":"height"} keyboardVerticalOffset={0}>{player&&data?<PlayerScreen matches={data.matches} id={player.id} side={player.side} onBack={()=>setPlayer(null)} onMatch={m=>{setPlayer(null);setMatch(m)}}/>:match&&data?<MatchScreen match={match} nickname={data.profile.nickname} onBack={()=>setMatch(null)} onPlayer={(id,side)=>setPlayer({id,side})}/>:data?<Home data={data} onMatch={setMatch} onPlayer={(id,side)=>setPlayer({id,side})}/>:<TouchableWithoutFeedback onPress={Keyboard.dismiss} accessible={false}><View style={s.flex}><ScrollView contentContainerStyle={s.landing} keyboardShouldPersistTaps="handled" keyboardDismissMode={Platform.OS==="ios"?"interactive":"on-drag"} automaticallyAdjustKeyboardInsets={Platform.OS==="ios"}><Text style={s.eyebrow}>MATCH INTELLIGENCE</Text><Text style={s.title}>내 플레이를{`\n`}숫자로 읽다.</Text><Text style={s.subtitle}>Android와 iOS에서 최근 경기와 선수 흐름을 확인하세요.</Text><Text style={s.inputLabel}>구단주명</Text><TextInput style={s.input} value={nickname} onChangeText={setNickname} placeholder="구단주명을 입력하세요" placeholderTextColor="#496055" returnKeyType="next"/><Text style={s.inputLabel}>Nexon Open API Key</Text><TextInput style={s.input} value={apiKey} onChangeText={setApiKey} placeholder="API 키를 입력하세요" placeholderTextColor="#496055" secureTextEntry autoCapitalize="none" returnKeyType="done" onSubmitEditing={()=>void load()}/><Pressable style={s.primary} onPress={load}>{loading?<ActivityIndicator color={C.bg}/>:<Text style={s.primaryText}>전적 불러오기 →</Text>}</Pressable>{error?<Text style={s.error}>{error}</Text>:null}<Text style={s.privacy}>API 키는 요청에만 사용되며 저장하지 않습니다.</Text></ScrollView></View></TouchableWithoutFeedback>}</KeyboardAvoidingView><View style={s.footer}><Text style={s.muted}>Data based on NEXON Open API</Text></View></SafeAreaView></SafeAreaProvider>}
